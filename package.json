{
  "name": "uyammadu-portfolio",
  "version": "1.0.0",
  "description": "Static personal portfolio and dev services site for dev.uyammadu.com.",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build:css": "sass sass/main.scss css/style.css --style=compressed --no-source-map",
    "build:static": "mkdir -p dist && cp -R *.html *.txt *.xml _redirects css assets index.js dist/",
    "build": "npm run clean && npm run build:css && npm run build:static",
    "dev": "sass sass/main.scss css/style.css --watch --no-source-map",
    "preview": "python3 scripts/preview.py"
  },
  "author": "Chukwuemelie \"Chuk\" Uyammadu",
  "license": "GPL-3.0",
  "devDependencies": {
    "sass": "^1.94.2"
  }
}
